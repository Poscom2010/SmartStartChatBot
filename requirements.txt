streamlit>=1.28.0
# Keep RetrievalQA path compatibility
langchain==0.2.16
langchain-community==0.2.16

# Use faiss and HF stack versions with widely available wheels on hosted envs
faiss-cpu==1.7.4
sentence-transformers==2.2.2
transformers==4.41.1
huggingface-hub==0.22.2

# Let sentence-transformers pull a compatible torch; avoid hard-pinning
# If needed later, we can pin a CPU wheel version based on the platform/python

python-dotenv>=1.0.0
streamlit-lottie>=0.0.5
Pillow==10.3.0
numpy==1.26.4
requests>=2.31.0

# Pydantic v1 to prevent 'fields_set' runtime errors
pydantic<2

# Together SDK compatible with community integration
together==0.2.12
